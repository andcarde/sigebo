
// config.js
'use strict';

const config = {
    // MySQL
    mysql: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'sigebo',
        port: 3306
    },

    // Server Port
    port: 3000,

    debugMode: true
}

module.exports = config;
