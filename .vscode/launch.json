{
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch Program",
        "program": "${workspaceFolder}/${input:programPath}",
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ],
    "inputs": [
      {
        "type": "pickString",
        "id": "programPath",
        "description": "Select the entry point for your application",
        "options": [
          "app.js",
          "tests/validate_bottling.test.js"
        ]
      }
    ]
  }